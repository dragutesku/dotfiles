# Install node & npm packages
# https://volta.sh/
volta install node;
volta install npm;
volta install -g typescript;
volta install -g eslint;
volta install -g http-server;
volta install -g nodemon;
volta install -g @angular/cli;
volta install -g next@latest;
volta install -g react@latest;
volta install -g react-dom@latest;
